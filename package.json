{
  "name": "foodgpt-monorepo",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd website && npm ci && npm run build",
    "start": "cd website && npm run start"
  }
}
